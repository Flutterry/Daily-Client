import 'package:daily_client/src/application.dart';

class CustomBottomNavigationSpacer extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox(height: getHeight(10));
  }
}
